🐍 Snake Reinforcement Learning

This project implements a Reinforcement Learning (RL) agent that learns to play the classic Snake game.
The environment, training loop, and neural network model are designed to let the agent improve through trial-and-error using reward signals based on game progress and survival.

11 state
